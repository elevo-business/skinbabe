{%- if block.settings.text != blank -%}
  <div class="reassure">
    {%- if block.settings.icon != 'none' -%}<span class="reassure__icon">{% render 'icon', icon: block.settings.icon %}</span>{%- endif -%}
    <p>{{ block.settings.text }}</p>
  </div>
{%- endif -%}
